{
	"scripts": {
		"dev": "bundle exec jekyll serve",
		"watch": "bundle exec jekyll serve -l",
		"build": "JEKYLL_ENV=production bundle exec jekyll build"
	},
	"devDependencies": {
		"autoprefixer": "^9.3.1",
		"postcss-cli": "^6.0.1",
		"postcss-import": "^12.0.1",
		"purgecss": "^1.1.0",
		"tailwindcss": "^1.0.0"
	}
}
